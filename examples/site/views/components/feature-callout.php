<article class="feature-callout" mint-attrs>
    <h3 class="feature-callout__title">{{ $title }}</h3>
    <p class="feature-callout__subtitle">{{ $subtitle }}</p>
    <div class="feature-callout__body">
        {{ $slot }}
    </div>
</article>
